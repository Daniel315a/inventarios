import { FiltroCotizacionesPipe } from './filtro-cotizaciones.pipe';

describe('FiltroCotizacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCotizacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
