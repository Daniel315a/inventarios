import { FiltroFacturasPipePipe } from './filtro-facturas-pipe.pipe';

describe('FiltroFacturasPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFacturasPipePipe();
    expect(pipe).toBeTruthy();
  });
});
