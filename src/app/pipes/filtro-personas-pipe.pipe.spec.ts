import { FiltroPersonasPipePipe } from './filtro-personas-pipe.pipe';

describe('FiltroPersonasPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPersonasPipePipe();
    expect(pipe).toBeTruthy();
  });
});
