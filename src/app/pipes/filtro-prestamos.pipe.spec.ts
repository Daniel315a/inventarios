import { FiltroPrestamosPipe } from './filtro-prestamos.pipe';

describe('FiltroPrestamosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPrestamosPipe();
    expect(pipe).toBeTruthy();
  });
});
